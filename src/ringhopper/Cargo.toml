[package]
name = "ringhopper"
version = "0.2.0"
license = "GPL-3.0-only"
edition = "2024"

[dependencies]
ringhopper-structs = { path = "../ringhopper-structs" }
spin = "0.10.0"

[features]
std = []

[lints]
workspace = true
