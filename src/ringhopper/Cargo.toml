[package]
name = "ringhopper"
version = "0.2.0"
license = "GPL-3.0-only"
edition = "2024"

[dependencies]
ringhopper-structs = { path = "../ringhopper-structs", default-features = false }
minxp = { version = "0.1.6", optional = true }
spin = "0.10.0"

[features]
std = ["ringhopper-structs/std"]
minxp = ["dep:minxp"]
default = []

[lints]
workspace = true
